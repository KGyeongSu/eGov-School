<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:p="http://www.springframework.org/schema/p"
	   xmlns:c="http://www.springframework.org/schema/c"
	   xsi:schemaLocation="
			http://www.springframework.org/schema/beans 
			https://www.springframework.org/schema/beans/spring-beans.xsd" >

	<!-- hikariCP라는 커낵션풀을 사용. 
		DB 접속에 필요한 정보를 모아놓는 설정 객체 -->
	<bean id="hikariConfig"
		  class="com.zaxxer.hikari.HikariConfig"
		  p:driverClassName="${jdbc.driverClassName}"
		  p:jdbcUrl="${jdbc.url}"
		  p:username="${jdbc.username}"
		  p:password="${jdbc.password}"
		  p:maximumPoolSize="${jdbc.maximumPoolSize}"
		  p:minimumIdle="${jdbc.minimumIdle}"
		  />
	
	<!-- dataSource 
		hikariConfig 설정을 받아서 실제 DB 커넥션들을 미리 만들어놓고 관리
		c:-re f="hikariConfig": 위에서 만든 hikariConfig 빈을 생성자로 넘겨줌. "이 설정대로 커넥션 만들어"라는 뜻-->
	<bean id="dataSource"
		  class="com.zaxxer.hikari.HikariDataSource"
		  destroy-method="close"
		  c:hikariConfig-ref="hikariConfig"
	      />
	      
	
	
	<!-- sqlSession + Factory -->
	<bean id="sqlSessionFactory"
		  class="org.mybatis.spring.SqlSessionFactoryBean"
		  p:dataSource-ref="dataSource"
		  p:configLocation="classpath:com/egov/school/config/sqlConfig.xml"
		  p:mapperLocations="classpath:com/egov/school/mappers/*-Mapper.xml"	
		  />
	<bean id="sqlSession"
		  class="org.mybatis.spring.SqlSessionTemplate"
		  c:sqlSessionFactory-ref="sqlSessionFactory"
		  />
	<bean id="transactionManager"
		  class="org.springframework.jdbc.datasource.DataSourceTransactionManager"		
		  p:dataSource-ref="dataSource"
		  />

</beans>