<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans 
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context 
        https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- BCrypt 암호화 Bean -->
	<!-- 회원가입 시 암호화, 로그인 시 비교에 사용 -->
	<bean id="encoder" 
     class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
    
    <!-- 로그인 성공 처리 Bean -->
    <bean class="com.school.security.LoginSuccessHandler"/>

    <!-- 로그인 실패 처리 Bean -->
    <bean class="com.school.security.LoginFailureHandler"/>

    <!-- 권한 없을 때 처리 Bean -->
    <bean class="com.school.security.Custom403Handler"/>
    
    <!-- 로그인 검사 Bean -->
    <!-- userService, encoder 두개를 생성자로 주입 -->
    <bean class="com.school.security.CustomAuthenticationProvider">
        <constructor-arg name="userService" ref="userService"/>
        <constructor-arg name="encoder" ref="encoder"/>
    </bean>
    
    <!-- security 패키지 컴포넌트 스캔 -->
    <!-- SpringSecurityConfig.java의 @Configuration 인식하기 위해 필요 -->
    <context:component-scan base-package="com.school.security"/>

</beans>
