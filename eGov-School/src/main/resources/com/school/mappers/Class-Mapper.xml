<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Class-Mapper">
	
	<select id="selectClassList" resultType="classs">
	
		select cla_num, cla_title, cla_goal, cla_thumb, user_name, cla_status, cla_startdate, cla_enddate, cla_maxstu, cla_views
		from class c
		inner join users u
		on c.user_num = u.user_num
		where c.user_num = #{userNum}
		order by c.cla_num desc
		
	</select>
	
	<select id="selectSearchClassList" resultType="classs">
	
		select cla_num, cla_title, cla_goal, cla_thumb, user_name, cla_status, cla_startdate, cla_enddate, cla_maxstu, cla_views
		from class c
		inner join users u
		on c.user_num = u.user_num
		<where> 
			c.user_num = #{userNum}
			
			<if test="pageMaker.keyword != null and pageMaker.keyword != ''">
				and cla_title like '%' || #{pageMaker.keyword} || '%'	
			</if>	
		</where>
		order by cla_num desc
	
	</select>
	
	<select id="selectSearchClassListCount" resultType="int">
	
		select count(*)
		from class
		<where> 
			user_num = #{userNum}
			<if test="pageMaker.keyword != null and pageMaker.keyword != ''">
				and cla_title like '%' || #{pageMaker.keyword} || '%'	
			</if>	
		</where>
	
	</select>
	
	<select id="selectClassByCla_num" resultType="classs">
	
		select cla_num, cla_title, cla_content, cla_goal, cla_category, cla_thumb, cla_complete, user_name, cla_status, cla_startdate, cla_enddate, cla_maxstu, cla_views
		from class c
		inner join users u
		on c.user_num = u.user_num
		where cla_num = #{claNum}
	
	</select>
	
	<insert id="insertClass" parameterType="classs">
	
		insert into class(cla_num, cla_title, cla_content, cla_goal, cla_category, cla_thumb, user_num, cla_startdate, cla_enddate, cla_maxstu, cla_complete)
		values (#{claNum}, #{claTitle}, #{claContent}, #{claGoal}, #{claCategory}, #{claThumb}, #{userNum}, #{claStartDate}, #{claEndDate}, #{claMaxStu}, #{claComplete})
	
	</insert>
	
	<update id="increaseViewCnt" parameterType="Str">
	
		update class set
		cla_views = cla_views+1
		where cla_num = #{claNum}
	
	</update>
	
	<select id="selectClassSeqNext" resultType="int">
	
		select
		class_seq.nextval as cla_num from dual
	
	</select>
	
</mapper>