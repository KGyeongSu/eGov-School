<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Lesson-Mapper">

	<select id="selectLessonSeqNext" resultType="str">
		SELECT SEQ_LESSON.NEXTVAL FROM DUAL
	</select>

	<insert id="insertLesson" parameterType="lesson">
		INSERT INTO LESSON(LSN_NUM, CLA_NUM, LSN_SEQ, LSN_TITLE, LSN_TIME)
		VALUES(#{lsnNum}, #{claNum}, #{lsnSeq}, #{lsnTitle}, #{lsnTime})
	</insert>

	<!-- 강좌별 차시 목록 -->
	<select id="selectLessonList" parameterType="str" resultType="lesson">
		SELECT * FROM LESSON
		WHERE CLA_NUM = #{claNum}
		ORDER BY LSN_SEQ ASC
	</select>

	<!-- 차시 단건 조회 -->
	<select id="selectLessonByNum" parameterType="str" resultType="lesson">
		SELECT * FROM LESSON
		WHERE LSN_NUM = #{lsnNum}
	</select>

	<!-- 차시 수정 -->
	<update id="updateLesson" parameterType="lesson">
		UPDATE LESSON
		SET LSN_TITLE = #{lsnTitle},
		    LSN_SEQ = #{lsnSeq},
		    LSN_TIME = #{lsnTime}
		WHERE LSN_NUM = #{lsnNum}
	</update>

	<!-- 차시 삭제 -->
	<delete id="deleteLesson" parameterType="str">
		DELETE FROM LESSON
		WHERE LSN_NUM = #{lsnNum}
	</delete>

</mapper>